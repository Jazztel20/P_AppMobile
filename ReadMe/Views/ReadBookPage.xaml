<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ReadMe.Views.ReadBookPage"
             Title="">
    <VerticalStackLayout>
        <WebView x:Name="BookWebView" HeightRequest="650">

        </WebView>
        <HorizontalStackLayout HorizontalOptions="Center" Spacing="5">
            <Button Text="Précédent" x:Name="previousBtn" Clicked="OnPreviousClicked" WidthRequest="200"/>
            <Button Text="Suivant" x:Name="nextBtn" Clicked="OnNextClicked" WidthRequest="200"/>
        </HorizontalStackLayout>
    </VerticalStackLayout>
</ContentPage>